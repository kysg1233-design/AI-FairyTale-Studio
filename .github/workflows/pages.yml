name: Publish studio to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'studio-v6.html'
      - 'style.css'
      - 'app.js'
      - 'app-studio-v6.js'
      - 'narration.js'
      - 'config.js'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy static studio
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Check JavaScript syntax before publishing
        run: |
          node --check narration.js
          node --check app.js
          node --check app-studio-v6.js
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Download pinned browser video engine
        run: |
          mkdir -p /tmp/fairytale-deps
          npm install --prefix /tmp/fairytale-deps --no-audit --no-fund @ffmpeg/ffmpeg@0.12.10 @ffmpeg/core@0.12.10
      - name: Collect ONLY public website files
        run: |
          mkdir -p site
          cp index.html studio-v6.html style.css app.js app-studio-v6.js narration.js config.js site/
          mkdir -p site/ffmpeg
          cp /tmp/fairytale-deps/node_modules/@ffmpeg/ffmpeg/dist/esm/*.js site/ffmpeg/
          cp /tmp/fairytale-deps/node_modules/@ffmpeg/core/dist/esm/ffmpeg-core.js site/ffmpeg/
          cp /tmp/fairytale-deps/node_modules/@ffmpeg/core/dist/esm/ffmpeg-core.wasm site/ffmpeg/
          touch site/.nojekyll
      - name: Upload static website
        uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
